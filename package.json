{
  "version": "0.0.1",
  "private": true,
  "name": "near-me-leaflet",
  "description": "near-me-leaflet",
   "license": "MIT",
  "devDependencies": {
    "bower": "latest",
    "http-server": "^0.6.1"   
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 8000"
  }
}
